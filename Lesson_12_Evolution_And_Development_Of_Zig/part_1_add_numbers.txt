const std = @import("std");

fn addNumbers(a: i32, b: i32) i32 {
    return a + b;
}

pub fn main() void {
    const result: i32 = addNumbers(5, 10);
    std.debug.print("Result: {}\n", .{result});
}
