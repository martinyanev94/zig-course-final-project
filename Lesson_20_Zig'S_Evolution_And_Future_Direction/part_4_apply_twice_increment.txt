const std = @import("std");

fn applyTwice(fnToApply: fn(i32) i32, value: i32) i32 {
    return fnToApply(fnToApply(value));
}

fn increment(x: i32) i32 {
    return x + 1;
}

pub fn main() void {
    const result = applyTwice(increment, 5);
    std.debug.print("Result: {}\n", .{result});
}
