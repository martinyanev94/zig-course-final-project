const std = @import("std");

pub fn main() void {
    var buffer: [5]u8 = undefined; // Safe as size is known
    for (i32(i := 0); i < 5; i += 1) {
        buffer[i] = i;
    }
    std.debug.print("Buffer: {:#?}\n", .{buffer});
}
