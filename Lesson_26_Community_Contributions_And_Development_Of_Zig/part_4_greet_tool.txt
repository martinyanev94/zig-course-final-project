const std = @import("std");

pub fn main() void {
    const args = std.process.args();
    if (args.len < 2) {
        std.debug.print("Usage: my_tool <name>\n", .{});
        return;
    }
    
    std.debug.print("Hello, {}!\n", .{args[1]});
}
