const std = @import("std");

pub fn enhancedRead() ![]u8 {
    const file = try std.fs.cwd().openFile("myfile.txt", .{});
    defer file.close();
    
    var buffer: [1024]u8 = undefined;
    const bytesRead = try file.read(buffer[0..]);
    
    return buffer[0..bytesRead];
}
